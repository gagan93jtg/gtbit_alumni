<% if @response == 'Password Updated !'%>
  $('.password_update_response_div').html("<div class='alert alert-success'>Password Updated !</div>");
<% else %>
  <% if @response.class == Array %>
    $('.password_update_response_div').html("<div class='alert alert-danger'>We have faced these error(s) while updating password" +
      "<ul class='active-record-errors'>" +
      "<% @response.each do |message| %>" +
      "<li class='active-record-error'><%= message %></li>" +
      "<% end %>" +
      "</ul>"+
      "</div>")
  <% elsif @response.class == String %>
  $('.password_update_response_div').html("<div class='alert alert-danger'><%= @response %>");
  <% end %>
<% end %>
